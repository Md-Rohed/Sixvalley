import Close from "@/icons/CloseIcon.svg";
import ArrowDown from "@/icons/arrowDown.svg";
import Twitter from "@/icons/twitter.svg";
import Facebook from "@/icons/facebook.svg";
import Instagram from "@/icons/instagram.svg";
import Youtube from "@/icons/youtube.svg";
import Phone from "@/icons/phone.svg";
import Logo from "@/icons/6vallyLogo.svg";
import Profile from "@/icons/profile.svg";
import Shuffle from "@/icons/shuffle.svg";
import Wishlist from "@/icons/wishlist.svg";
import Cart from "@/icons/cart.svg";
import Search from "@/icons/search.svg";
import Menu from "@/icons/menu.svg";
import RightArrow from "@/icons/rightArrow.svg";
import AppDownloadLink from "@/icons/appDownloadLink.svg";

export function AppDownloadLinkIcon({ className }) {
  return (
    <AppDownloadLink
      className={`h-[0.75rem] w-[0.75rem] text-black ${className} `}
    />
  );
}

export function RightArrowIcon({ className }) {
  return (
    <RightArrow
      className={`h-[0.75rem] w-[0.75rem] text-black ${className} `}
    />
  );
}

export function ProfileIcon({ className }) {
  return (
    <Profile className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}
export function PhoneIcon({ className }) {
  return (
    <Phone className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}

export function YoutubeIcon({ className }) {
  return (
    <Youtube className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}

export function MenuIcon({ className }) {
  return (
    <Menu className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}

export function SearchIcon({ className }) {
  return (
    <Search className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}

export function CloseIcon({ className }) {
  return (
    <Close className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}

export function ShuffleIcon({ className }) {
  return (
    <Shuffle className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}

export function WishlistIcon({ className }) {
  return (
    <Wishlist className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}

export function CartIcon({ className }) {
  return (
    <Cart className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}

export function ArrowDownIcon({ className }) {
  return (
    <ArrowDown className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}
export function TwitterIcon({ className }) {
  return (
    <Twitter className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}
export function FacebookIcon({ className }) {
  return (
    <Facebook className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}

export function InstagramIcon({ className }) {
  return (
    <Instagram className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}

export function LogoIcon({ className }) {
  return (
    <Logo className={`h-[0.75rem] w-[0.75rem] text-black ${className} `} />
  );
}
